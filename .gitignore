node_modules/
.env
*.db
*.db-shm
*.db-wal
desktop/
mobile/

# iOS coverage reports — docs/COVERAGE.md is committed; raw data is not.
ios/build/
*.xcresult

# Personal dev-env launch config (e.g. hard-coded home paths for node).
# launch.json contents are developer-specific, not team-wide.
.claude/launch.json
